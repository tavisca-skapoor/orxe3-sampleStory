import { LitElement, TemplateResult } from 'lit-element';
import { Instance } from '@bit/orxe_3.base.orxe-flatpickr/types/instance';
import { DateOption } from './datepicker-types';
import '@bit/orxe_3.base.input-container';
export default class OrxeDatePicker extends LitElement {
    calender: Instance;
    calenderId: string;
    datepickerInput: string;
    datepickerRangeInput: string;
    backArrowId: string;
    calenderPanelId: string;
    mode: 'single' | 'range';
    disableMobile: boolean;
    allowInput: boolean;
    defaultDate: string;
    dateFormat: string;
    enable: Array<DateOption>;
    disable: Array<DateOption>;
    inline: boolean;
    minDate: DateOption;
    maxDate: DateOption;
    clickOpens: boolean;
    nextArrow: string;
    prevArrow: string;
    ariaDateFormat: string;
    position: 'auto' | 'above' | 'below';
    weekNumbers: boolean;
    firstUpdated(): void;
    private _renderDatePicker;
    private _rangeDatepickerHtml;
    private _singleDatepickerHtml;
    render(): TemplateResult;
    createRenderRoot(): this;
}
